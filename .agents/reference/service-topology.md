# Service Topology

This is the source-anchored ownership map for Dreamux service-layer objects.
Read current source first when behavior matters; this page records who is
allowed to build and hold each service object so future refactors do not
rediscover issue #233 by trial and error.

## Ownership Map

| Service object | Owner / construction sites | Holds / owns | Scope | Depends on / direction |
|---|---|---|---|---|
| `Server` admin admission | `createAdminSocketServer` is constructed by server wiring in `/packages/dreamux/src/server.ts`; admin socket request execution enters `Server.admitAdminRequest()` in `/packages/dreamux/src/admin/socket.ts`. | Process-level admin request admission and the in-flight admin request set. | Whole process. | Shutdown closes admin and dispatcher-factory admission synchronously, closes dispatcher ownership trees first, then drains accepted admin requests and closes the socket. Admin handlers should not bypass this capability when they may materialize dispatchers or mutate state. |
| `Dispatchers` | Constructed by server wiring at `/packages/dreamux/src/server.ts:155`; inside the service package it is the process collection in `/packages/dreamux/src/service/dispatchers/index.ts:34`. It constructs `DispatcherService` at `/packages/dreamux/src/service/dispatchers/index.ts:71`. It holds a **separate read-only `AgentIdentityStore`** at `/packages/dreamux/src/service/dispatchers/index.ts:62` for unmaterialized summary/status probes — it does not borrow the per-dispatcher store from a live `DispatcherService`. | A `Map<string, DispatcherService>`, restart-intent consumer, all-settled process shutdown sweep, factory-admission fence, and a read-only identity reader for dispatcher-global name listing. It owns no teammate/team/channel/router state. | Process-global collection over dispatchers. | May depend on config, `DispatcherStore`, provider catalogs, and logging. It must only hand each dispatcher id to `DispatcherService`; it must not reach into per-dispatcher internals. Shutdown closes the dispatcher factory before taking the sweep snapshot, so `get()` cannot materialize a new aggregate after process shutdown starts. |
| `DispatcherService` | Constructed only by `Dispatchers.get()` in `/packages/dreamux/src/service/dispatchers/index.ts`. Its constructor assembles the per-dispatcher graph in `/packages/dreamux/src/service/dispatcher-service/index.ts`. | Stateless `CompletionDeliveryPolicy`, dispatcher-scoped core event bus, dispatcher-scope `TeammateCollection`, `TeamCollection`, shared stores, shared worktree manager, `ChannelService`, dispatcher scheduler, admission drain, `DispatcherInputSourceLifecycle`, `TeamChannelCoordinator`, and collaboration-space facade. | One aggregate per dispatcher. | Depends on provider catalogs, `DispatcherStore`, channel catalog, and service collaborators. It owns dispatcher-global topology and close-before-drain ordering: stop Workflows, canonically close durable Teams and ordinary TeamMates, then drain channels, starts, collaboration work, and admitted tasks. Core stays behind `AgentRuntimeProvider` and `ChannelProvider` seams. |
| `DispatcherInputSourceLifecycle` | Constructed by `DispatcherService`; class lives in `/packages/dreamux/src/service/dispatcher-service/input-source-lifecycle.ts`. | Preparation/start single-flight promises, the prepared Channel-session set, started state, dispatcher agent/workspace publication, ordered live Channel publication, and failed-start rollback. | One lifecycle capability per dispatcher aggregate. | Receives the aggregate's constructed collaborators and availability fact once. It creates fresh event and strict-route leases before each Channel session starts and publishes a session only after provider start succeeds. Failed start revokes admission/leases, drains accepted work, and sweeps materialized Team runtimes. Aggregate stop interrupts Team dissolve before waiting for this lifecycle, then retains the cross-owner cleanup sequence. |
| `DispatcherTaskDrain` | Constructed by `DispatcherService`; class lives in `/packages/dreamux/src/service/dispatcher-service/inbound-task-drain.ts`. | The dispatcher-owned admission gate and pending-task set for work that may create runtime, scheduler, route, or durable state, including runners published after durable Team-dissolve acceptance. | One gate per dispatcher aggregate. | Stop/shutdown closes admission and interrupts cancellable Team-dissolve waits before awaiting input-source recovery, then signals again before draining admitted work and sweeping the ownership tree. It does not own domain state; it only fences entry and completion. |
| `CompletionDeliveryPolicy` | Constructed by `DispatcherService` in `/packages/dreamux/src/service/dispatcher-service/index.ts`; implementation remains in `/packages/dreamux/src/service/completion-router/index.ts`. | Stateless preparation plus bounded submission attempts for an already-captured completion initiator. | One reusable policy per dispatcher. | The initiating action captures a closure on the entity-owned Turn. The policy retries only explicit provider-proven pre-admission failure; throw, timeout, unsupported, accepted, and ambiguous admission are terminal. It owns no Turn registry, key map, or replay state. |
| `DispatcherCoreEventBus` | Constructed only by `DispatcherService`; implementation lives in `/packages/dreamux/src/service/dispatcher-core-events/`. | An in-process `EventEmitter`, a narrow internal publisher capability, and revocable read-only Channel event sources. It retains no events or domain state. | Exactly one bus per dispatcher aggregate; one source lease per live Channel session generation. | Team, identity, channel-binding, and collaboration-space owners publish the allowlisted state/binding DTOs after their normal writes. Turn submitted/settled events do not exist. Channel providers receive only the public source from `@excitedjs/dreamux-types`, never the bus, publisher, or raw listener-management surface. Listener failures are isolated; stop and failed start revoke the session generation. |
| `WorktreeManager` | Constructed by `DispatcherService`; implementation lives in `/packages/dreamux/src/service/worktree/manager.ts`. | Worktree preparation, non-destructive dirty/unmerged cleanup assessment, and cleanup mutation; it is stateless apart from filesystem effects. | Per dispatcher helper shared by team and teammate collections. | `TeamCollection` calls `assessCleanup()` before durable dissolve acceptance and again after Team-wide idle. `cleanup()` reassesses immediately before mutation, performs no ref enumeration or history walk, and removes a clean managed `delete-on-close` worktree only with non-forced `git worktree remove <path>`. It preserves the managed branch and commits; branch/ref deletion is outside this capability. `cleanup: keep` and non-managed workspaces are terminally retained. Path derivation remains in platform/worktree helpers. |
| `ChannelBindingStore` | Constructed by `ChannelService` at `/packages/dreamux/src/service/channel-service/index.ts:59`; standalone syntax preflight exists at `/packages/dreamux/src/service/channel-binding/store.ts:392`, and v2/collaboration overlap preflight lives in `/packages/dreamux/src/service/channel-binding/preflight.ts:18`. | Persistent channel binding records, including the opaque managed-route `claim_id` cleared by explicit binds. | Per dispatcher data, with stateless store object. | `ChannelService` consumes this store; Team services and collections do not read or mutate binding rows directly. Binding store v3 reuses v2 rows with `(channel_id, target_key)` as explicit routes (`claim_id: null`) only when the preflight finds no open collaboration target with the same route key; ambiguous overlap fails loud. Collaboration reconciliation releases a route only through its exact claim, never by inferring provenance from the Team owner tuple. |
| `AgentIdentityStore` | Constructed by `DispatcherService` and injected into collections. `Dispatchers` builds a **separate read-only instance** at `/packages/dreamux/src/service/dispatchers/index.ts:62` for path probes; it does not borrow the per-dispatcher store. Collections receive the shared store; they do not build their own. | Reads and writes `identity.json` for the dispatcher root agent, dispatcher teammates, team leaders, and team members. Identity owns lifecycle/worktree/intent/role guidance plus the runtime-native session id and opaque nullable transcript locator; it owns no rolling conversation projection. The live dispatcher instance also receives the narrow core-event publisher. | Stateless store over dispatcher/team/entity paths. | Path placement is derived from identity role and `team_id`; collections do not invent paths. Generated agent allocation scans persisted entity directory names, and identity creation is an atomic no-clobber write. After successful writes it publishes only TeamLeader/TeamMate status changes; dispatcher and standalone teammate identities are outside the Channel DTO allowlist. |
| `readAgentTranscript` | Called only by `TeammateCollection.last()`; implementation lives in `/packages/dreamux/src/service/agent-entity/transcript-reader.ts`. | Validates the neutral query, resolves the selected provider, delegates one cold `readTranscript()` call, verifies fixed output/page/block limits, projects provider-neutral rows, and classifies typed transcript errors. | Stateless read capability over one persisted identity and provider catalog. | It reads identity/config only, starts/materializes no runtime, performs no state write, and stores no cursor/cache/index. Provider-native paths and formats stay inside the provider package; core exposes the path only through direct spawn/send receipts. |
| `AgentRuntimeStateStore` | Constructed per-entity by `TeammateService` (not from the composition root). Class at `/packages/dreamux/src/service/agent-entity/runtime-state.ts:12`. | Bridges runtime state callbacks (`AgentRuntimeStateCallbacks`) to the entity's `AgentIdentityStore` row. Updates intent/status and atomically persists runtime session id plus nullable transcript locator. | Per agent entity (dispatcher agent, teammate, team leader, team member). | Depends on the shared `AgentIdentityStore` and the entity's current identity snapshot. It does not persist to its own file; it writes through to `identity.json`. |
| `ChannelService` | Constructed by `DispatcherService` in `/packages/dreamux/src/service/dispatcher-service/index.ts`. It wraps the private `ChannelSessions` helper in `/packages/dreamux/src/service/channel-service/channel-sessions.ts`. | Live `Map<channel_id, ChannelSession>`, channel-tool forwarding, target resolution, channel MCP descriptor assembly, `ChannelBindingStore`, binding summaries, bind/transfer writes, inbound binding lookup, and TeamLeader egress ownership checks. | Per dispatcher. | Depends on `ChannelProviderCatalog`, channel config, its internally owned `ChannelBindingStore`, and the core-owned channel MCP descriptor renderer in `service/channel-service/mcp-descriptors.ts`. It owns channel/binding facts only; Team route owners are flat routing data and Team lifecycle remains outside it. |
| `CollaborationSpaceService` | Constructed by `DispatcherService`. Facade, target lifecycle worker, target-close handoff capability, and route reconciler live in `/packages/dreamux/src/service/collaboration-space/`. | Collaboration-space bind/dissolve/status/list facade, atomic binding transitions, lifecycle-task tracking/drain, and target provisioning/close/route reconciliation. Its shared route lock is keyed by `(channel_id, target_key)`. `target-close-lifecycle.ts` persists an exact handoff under that lock, releases it while awaiting `logicalClosed`, and generation-checks final target close under a reacquired lock. | One dispatcher-local service plus contained target-lifecycle, target-close, and route-reconciliation helpers. | Depends on `ChannelService`, dispatcher-level `TeamCollection`, config, and `CollaborationSpaceStore`. It owns no Team dissolve state machine or agent runtime. Operations that also need a Team lease take the target lock first; scoped transfer validates its descriptor generation inside that lock. Route sweeps validate handoffs against the authoritative TeamCollection record after lock handoff; accepted Team lifecycle still starts if the target-side operation-id correlation write fails. |
| `createDispatcherAgent` | Called by `DispatcherInputSourceLifecycle.doPrepareChannels()` in `/packages/dreamux/src/service/dispatcher-service/input-source-lifecycle.ts`; defined in `/packages/dreamux/src/service/dispatcher-service/agent.ts`. | Builds the dispatcher-owned agent as a contained `TeammateService` over the root dispatcher identity. | One dispatcher agent per prepared `DispatcherService`. | Depends on dispatcher config, the shared identity store, completion-delivery policy, and static MCP descriptors. It must not construct `TeammateService` directly. |
| Dispatcher `SchedulerService` | Constructed privately by `DispatcherService` in `/packages/dreamux/src/service/dispatcher-service/index.ts`; the public dispatcher getter returns `SchedulerCommands`. | Dispatcher cron timers and durable cron jobs via its owned `CronJobStore`. | One scheduler service plus one external command surface for the dispatcher conversational agent. | It submits scheduled input to the dispatcher agent and receives `DispatcherService.admitOperation()` as its owner gate. Scheduler construction and lifecycle verbs belong to the container, not to `TeammateService`; external cron commands and timer fires must not bypass owner admission. |
| Dispatcher-scope `TeammateCollection` | Constructed by `DispatcherService` in `/packages/dreamux/src/service/dispatcher-service/index.ts`; admin-facing mutating ops are wrapped by the `teammateOps` admission surface there. | Dispatcher-owned teammate entities, shared identity store, worktree manager, provider-backed cold reads, and per-name live `TeammateService` cache. | One collection per dispatcher with `teamScope: null`. | May create/cache ordinary `teammate` entities only. It uses `createTeammateService`; it must not know team-leader launch policy or provider transcript format. |
| `TeamCollection` | Constructed by `DispatcherService`; `runtime-registry.ts`, `read-model.ts`, `dissolve-controller.ts`, and `dissolve-runner.ts` are private capabilities of this aggregate. | Team store, create/rebuild cache, owner-only scheduler lifecycle, route and generation leases, materialized-service shutdown set, and the single durable Team dissolve lifecycle. The controller owns the immutable operation/TeamLeader generation, authoritative current-operation refresh, process-local operation registry, retry/recovery orchestration, `logicalClosed` settlement, fence finalization, registry removal, shutdown suspension, and terminal logging. The runner is a stateless accepted-phase executor and owns none of those terminal actions. | One collection per dispatcher. | Depends on the shared identity store, `WorktreeManager`, and dispatcher-owned launch/drain primitives. Every turn-producing or mutating Team path re-enters its availability gate; reads use a separate generation-checked lease. It creates/rebuilds `TeamService` but leaves leader/member construction and resource shutdown to that entity. Captured writers expose only name plus neutral `waitIdle`; dissolve lifecycle never receives a full runtime. It never duplicates dissolve state in MCP, admin, or provider code. |
| `TeamLeaderHandle` | Declared in `/packages/dreamux/src/service/dispatcher-service/team-leader-handle.ts`; created only by `DispatcherService.team()`. | A generation-bound TeamLeader member/workflow surface and `spawnTeamMate()` for the admin/MCP team-leader target. | Per resolved Team generation. | It is the only member/workflow surface returned by `DispatcherService.team()`. Mutations re-enter dispatcher admission and the Team availability lease; reads use a generation-checked read lease and remain available during dissolve. It does not expose concrete `TeamService`, raw `teammates.spawn`, scheduler lifecycle, leader runtime, route mutation, or a second dissolve implementation; scoped dissolve uses the descriptor-bound coordinator path. |
| `TeamChannelCoordinator` | Constructed by `DispatcherService`; class lives in `/packages/dreamux/src/service/dispatcher-service/team-channel-coordinator.ts`. | Caller-policy coordinator for Dispatcher and descriptor-scoped TeamLeader calls to the same Team dissolve lifecycle, accepted-receipt projection, exact-generation TeamLeader channel-tool leasing, and channel bind/transfer-back operations across `TeamCollection`, `ChannelService`, and `CollaborationSpaceService`. | Per dispatcher helper under `DispatcherService`. | It owns no route or dissolve state. Dispatcher admission captures the public-entry timestamp; the coordinator derives the absolute pre-acceptance deadline, forwards it to the lifecycle, and returns the durable receipt without observing `logicalClosed`. TeamLeader channel tools run inside `TeamCollection.withTeamLeaderLease`, while Dispatcher tools remain lease-free. Route operations delegate to collaboration reconciliation for lock ordering and provenance. |
| `stopTeamRuntimes` | Called during process shutdown and failed Channel start rollback; helper lives in `/packages/dreamux/src/service/dispatcher-service/team-runtime-stop.ts`. | Best-effort wrapper around `TeamCollection.stopAll()`. | Per shutdown or failed-start rollback attempt. | `TeamCollection.stopAll()` lists every durable non-closed Team, canonically materializes it, and asks each `TeamService` to stop Workflows and close its normal entities. The wrapper logs errors and returns them so sibling cleanup continues; it is not a raw-runtime sweep. |
| `TeamService.createNew` | Called only by `TeamRuntimeRegistry.create()` in `/packages/dreamux/src/service/team-collection/runtime-registry.ts`; implemented in `/packages/dreamux/src/service/team-service/index.ts`. | Creates the durable team record, leader identity, leader agent, optional first turn, team scheduler, per-team member collection, and a scheduler lifecycle capability returned only to the registry owned by `TeamCollection`. | One live team entity per open team. | Depends on `TeamServiceDeps` forwarded by the TeamCollection runtime registry. It owns team-root leader policy and team-member collection scope. |
| `TeamService.rebuild` | Called only by `TeamRuntimeRegistry.rebuild()` in `/packages/dreamux/src/service/team-collection/runtime-registry.ts`; implemented in `/packages/dreamux/src/service/team-service/index.ts`. | Rehydrates a live team service from a stored `TeamRecord`, including leader, scheduler, and the owner-only scheduler lifecycle capability. | One live team entity per cached team. | Depends on the shared identity store for the leader probe. It must fail loud if the stored leader is missing or not `team_leader`. |
| `TeamService` | Private constructor in `/packages/dreamux/src/service/team-service/index.ts`; only reachable through `createNew` and `rebuild`. | Own Team record projection, contained TeamLeader, team-scoped member collection, workflows, scheduler, live-writer inventory, and shared-worktree identity propagation. It logically closes owned resources and persists closed/cleanup-pending borrower state for an already accepted operation. | Per Team. | Depends on TeamCollection-provided stores, completion-delivery policy, worktree manager, availability gate, and MCP descriptors. Resource close stops Workflows first, materializes every durable non-closed member, then invokes each normal entity close. It does not accept dissolve, assess/delete the worktree, detach routes, or own retry state. |
| Team `SchedulerService` | Constructed privately by `TeamService`; its lifecycle capability is held by `TeamCollection`, while callers receive only `SchedulerCommands`. | TeamLeader cron timers and durable Team cron jobs. | One scheduler service per Team. | Public mutations and final timer fire enter dispatcher admission and the shared Team availability gate. Dissolve closes scheduler admission immediately and stops it; a safe pre-logical unwind restarts persisted jobs only after all Team admission owners reopen successfully. |
| Team-scoped `TeammateCollection` | Constructed by `TeamService`. | Team-member construction, shared identity store, canonical per-name materialization, exact-object cache subscriptions, roster queries, and provider-backed cold transcript reads. | One collection per Team with `teamScope: team_id`. | It may create/cache `team_member` entities only. Mutations enter the owning Team availability gate. Lifecycle callers use materialization as a factory/query capability and then command `TeammateService` or a restricted locked handle directly; the Collection owns no membership registry, bulk close, runtime sweep, or post-close command bookkeeping. |
| `createTeamLeaderAgentForTeam` / `createTeamLeaderAgent` | `TeamService.buildLeader()` calls the Team-specific assembler in `/packages/dreamux/src/service/team-service/leader-agent.ts`; that assembler calls `createTeamLeaderAgent`, which calls `createTeammateService`. | Assembles Team MCP/cron/channel descriptors, bundled skills, prompt policy, and then builds the leader as a contained `TeammateService`. | One leader per `TeamService`. | Depends on team-owned options and shared stores. The generic factory path must remain the sole constructor and must not construct `TeammateService` directly. |
| `createTeammateService` | Called for dispatcher agent, team leader, and member/ordinary teammates; defined in `/packages/dreamux/src/service/teammate-service/factory.ts`. | Composes identity/options with the single `TeammateServiceDeps` dependency contract before constructing a `TeammateService`. | Factory path for every conversational agent entity. | Depends on neutral runtime provider catalog, shared stores, optional worktree manager, and lifecycle/delivery capabilities. Runtime launch is resolved uniformly from `identity.agent_runtime -> agents[]`. |
| `TeammateService` | Constructed only by `createTeammateService`; class lives in `/packages/dreamux/src/service/teammate-service/index.ts`, with raw runtime authority in `runtime-owner.ts` and object Turn coordination in `turn-coordinator.ts` / `turn-recording.ts`. | One identity, mutation admission, process-local lock and restricted handle, lazily started runtime, canonical in-process Turns, one-shot outcome/delivery convergence, status, close single-flight, and committed retirement fact. | Per agent entity: dispatcher agent, dispatcher teammate, team leader, or team member. | It is the sole lifecycle command owner. It depends on `AgentRuntimeProviderCatalog` through the neutral runtime contract and must not import Collection internals, own `SchedulerService`, Team topology, Channel sessions, or native transcript parsing. `close()` stops runtime, drains admissions and bounded delivery, and commits durable closed; lock release is a separate owner action. |
| `SchedulerService` / `SchedulerCommands` | `SchedulerService` is defined in `/packages/dreamux/src/service/scheduler/service.ts`; `SchedulerCommands` and the scheduler request/options contracts are owned by `/packages/dreamux/src/service/scheduler/types.ts`. The service is constructed only by the dispatcher and Team containers. | Cron job store, timers, held-fire tokens, stop waiters, lifecycle verbs (`start` / `stop` / `deleteStoreFile`) on the private service, and create/update/delete behavior on the external command surface. | Per conversational-agent container. | Depends on a required owner-provided admission gate, narrow writer-idle capability, and scheduled-submit callback. `SchedulerCommands` exposes only `list` / `create` / `update` / `delete`; timer-triggered dispatch goes through the owner gate, and admission-ambiguous submission is recorded once without automatic retry. |

## Ownership Rules

- Cron ownership is on the conversational-agent container: dispatcher cron is
  built by `DispatcherService`, team-leader cron is built by `TeamService`, and
  `TeammateService` carries no `SchedulerService`. The construction sites in
  `/packages/dreamux/src/service/dispatcher-service/index.ts` and
  `/packages/dreamux/src/service/team-service/index.ts` are the current source
  anchors for this ownership rule.
- Every conversational agent goes through `createTeammateService`; `new
  TeammateService(...)` is allowed only in
  `/packages/dreamux/src/service/teammate-service/factory.ts:19`.
- The team leader is held by `TeamService`, not by the members collection.
  `TeammateCollection.assertInCollection` admits dispatcher `teammate` or team
  `team_member` only, and a team-scoped collection must reject a `team_leader`
  read-by-name.
- `TeamCollection` owns team registry/cache behavior; `TeamService` owns team
  entity behavior, leader policy, and member collection scope. The topology
  source anchors above are the current enforcement evidence.
- Channel binding creation, lookup, summaries, transfer-back, and TeamLeader egress checks are owned by `ChannelService`; `TeamService`, `TeamMateService`, and `TeamCollection` do not own binding-store access. Core runtime and channel operations stay behind neutral seams:
  `AgentRuntimeProvider` / `AgentRuntimeProviderCatalog` for agents and
  `ChannelProvider` / `ChannelProviderCatalog` for channels. Service objects
  assemble capabilities; they do not branch on provider internals.
- Shared shutdown aggregation belongs in `/packages/dreamux/src/service/shutdown-errors.ts`.
  `Server`, `Dispatchers`, and `DispatcherService` use it to attempt every owned
  cleanup stage before surfacing aggregate failures.
- Process-level admin request admission belongs to `Server`, while dispatcher
  aggregate factory admission belongs to `Dispatchers`. Shutdown must close
  both before the dispatcher ownership-tree sweep, otherwise an already
  connected admin client can materialize a new dispatcher after the sweep
  snapshot.

### Infrastructure Capability Injection Principle

Global infrastructure capabilities — logger, config, paths, provider catalogs —
must **not** be hand-sliced at call sites with ad-hoc `Pick` types or
`.bind()` adapters (e.g. the former
`new AgentIdentityStore({ warn: opts.log.warn.bind(opts.log) })`).
Instead, inject the stable capability object directly or use a named
factory/adapter owned by the capability module.

Rationale: a `Pick<DreamuxLogger, 'warn'>` + `.bind()` at each construction
site is manual adapter glue that (a) repeats at every call site, (b) obscures
the real dependency in type signatures, and (c) cements a bad implementation
when documented as intentional. Stores accept the full `DreamuxLogger` and
call only `this.log.warn(...)` internally; the discipline of "only warn from
stores" is enforced by review and convention, not by a compiler-narrowed
type at the injection boundary.

### Store Construction Patterns

Three patterns coexist. Each follows the ownership of its data; do not "unify"
them — matching construction to data scope is the point:

- **Shared from composition root.** `AgentIdentityStore` is built once by
  `DispatcherService`
  (`/packages/dreamux/src/service/dispatcher-service/index.ts`) with its narrow
  core-event publisher and injected into all collections and services. Identity
  records are dispatcher-global data, so the store must be a single shared
  instance. The publisher only reports allowlisted post-write facts; it does
  not own or persist the data. Detailed conversation history is provider-owned
  and enters core only through stateless cold `readTranscript()` delegation.
  `Dispatchers` builds its own separate read-only `AgentIdentityStore`
  (`/packages/dreamux/src/service/dispatchers/index.ts:62`) for unmaterialized
  summary/status probes without waking a `DispatcherService`.
- **Self-built inline by container.** `CronJobStore` is built inline by each
  scheduler owner (`DispatcherService` or `TeamService`). It is not shared because cron
  job state is per-scheduler (dispatcher cron vs. team cron are independent).
- **Per-entity built.** `AgentRuntimeStateStore` is built per `TeammateService`
  entity (`/packages/dreamux/src/service/teammate-service/index.ts:153`). It
  bridges runtime state callbacks to the entity's own identity row; sharing
  would break the per-entity identity binding. It does not persist to its own
  file — it writes through to `identity.json` via the shared `AgentIdentityStore`.

When adding a new store, ask: *who owns the data this store reads/writes?*
Match the construction pattern to the data scope, not to the nearest class.

### Source vs. Intent in This Document

Code is current behavior. This KB distinguishes three layers:

- **Current source facts** — what the code does today (e.g. "`AgentIdentityStore`
  constructor takes `DreamuxLogger`").
- **Architectural intent** — the target shape the code is moving toward or
  holding (e.g. "the identity store is shared from the composition root,
  not self-built by each collection").
- **Known debt / review heuristics** — things that work but should not be
  copied (e.g. "do not hand-slice infrastructure capabilities at call sites;
  inject the stable object or use a named factory").

When code and this document disagree, read the source first — then update the
document if the code moved intentionally, or fix the code if it drifted.

## Update Rules

When moving a service class, changing who constructs it, or changing ownership
scope, update this file in the same change and keep every cited
package source path resolvable. `.agents/scripts/check.sh` validates
the cited file paths so this page cannot drift into dead anchors silently.
