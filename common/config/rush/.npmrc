# pnpm config for the dreamux monorepo (consumed by rush update / pnpm install).
#
# `shamefully-hoist=false` keeps pnpm's strict module layout, which means
# packages can only resolve what they declare in their own package.json —
# accidental implicit dependencies on hoisted modules fail loudly.
shamefully-hoist=false
