{
  "name": "@excitedjs/feishu-channel",
  "entries": [
    {
      "version": "6.0.0",
      "tag": "@excitedjs/feishu-channel_v6.0.0",
      "date": "Fri, 04 Sep 2026 10:24:24 GMT",
      "comments": {
        "major": [
          {
            "comment": "BREAKING: Review: Automatic inbound received/in-progress reactions are removed and replaced by Feishu COT conversation cards. The RECEIVED_REACTION_EMOJI and IN_PROGRESS_REACTION_EMOJI exports, FeishuBot.removeReaction method, FeishuChannelSession.state field, and FeishuChannelState type are removed: stop importing the constants; callers must stop calling FeishuBot.removeReaction; fresh object-literal implementations must omit it, while class implementations should drop the now-unused method; readers must stop accessing session.state or naming FeishuChannelState. The model-facing react tool and addReaction surface remain available. Team-member turn events are ignored by Feishu COT rendering. No rebuild is needed."
          },
          {
            "comment": "BREAKING: Review: Feishu COT cards are now keyed by recipient rather than by Feishu target. Every TeamLeader and the Dispatcher owns exactly one standing anchor and at most one open card, wherever the anchor came from. A Feishu inbound becomes that anchor when the Channel submits it: the superseded open card closes as interrupted and its successor opens under the new message before Core runs. Only rejected, failed, and stopped outcomes retire the optimistic anchor; submitted, duplicate, ambiguous, and unknown-error outcomes keep it, so a duplicate delivery remains anchored while its earlier admitted turn continues. The Channel recognizes its own submitted turn from the existing caller-supplied `source_id` and hides that turn's already-visible user body once; every other producer's body displays normally. A card closes only when the runtime reports the end of its one native turn (completed closes as done; failed or interrupted closes as interrupted); that fact closes an already open card and never opens one, so it is ignored when no card is open. `teammate.turn.settled` no longer closes, reopens, or re-anchors anything. A card create or append the platform refuses abandons only that card: the standing anchor survives, and the next opening activity tries to open a card there again. A Reply is outbound only and never creates, replaces, or retires an anchor. A visible Team bind card may initialize a TeamLeader that has no standing anchor and never replaces an existing one; the Dispatcher has no installation or restart anchor. A tool result that arrives after an anchor replacement may be omitted. Anchors and cards live only in the live session's memory and are lost on restart by design, with no replay or backfill. The package's public export surface is unchanged; the target-keyed and dispatcher-scoped COT presentation modules this replaces were always internal. No transport, configuration, persisted state, or path shape changed and no rebuild is required."
          },
          {
            "comment": "BREAKING: Review: A Feishu inbound now becomes the selected Dispatcher or TeamLeader's COT anchor immediately when the Channel submits it, interrupting the prior card and opening the successor receipt before Core runs. Only rejected, failed, and stopped outcomes retire that optimistic anchor; submitted, duplicate, ambiguous, and unknown-error outcomes keep it, while unsubmitted means no Core submission anchor was taken before Dispatcher fallback. In particular, a duplicate delivery keeps the repeated message anchored while its already-admitted turn continues. The Channel recognizes its own turn from the existing caller-supplied `source_id` echoed on `teammate.turn.submitted` and hides that turn's already-visible user body exactly once; other producers' bodies remain visible. Correlation is bounded and released. Body-text matching is deliberately not used because path, secret, or truncation sanitization can change projected text and silently defeat suppression. No config or persisted-state shape changes, and no rebuild is needed."
          },
          {
            "comment": "BREAKING: creating a Feishu channel session without an explicit state root now fails instead of falling back to the process working directory. Previously an omitted `state_root` silently wrote the session's durable routing state — bindings, spaces, dispatcher access — into whatever directory the process happened to start in, a silent-data-corruption footgun that surfaced only later as state that had disappeared. `createSession` now throws immediately with a message naming the requirement. The published context type still marks `state_root` optional, so this is a runtime-behavior break rather than a type break. Dreamux hosts always inject a per-dispatcher state root and are unaffected; an embedder that relied on the implicit fallback must now provide `state_root` explicitly. No config, state, or path shape changed and no rebuild is required."
          },
          {
            "comment": "BREAKING: Rebuild: recreate every Feishu binding and collaboration-space policy with this Channel's own MCP tools after upgrading; Core's previous binding state is not migrated. This Channel now owns routing end to end: a durable per-channel binding document under the dispatcher state dir, direct and hierarchical target resolution, stale-binding cleanup, and automatic collaboration-space provisioning composed from ordinary `team.create` and `team.submit` Commands. Its MCP catalog is caller-scoped and replaces the removed Core tools: `bind_channel`, `unbind_channel`, and `list_bindings` for the Dispatcher, self-scoped `bind_channel`/`unbind_channel` for a TeamLeader, and the four collaboration-space policy tools for the Dispatcher; there is no `transfer_back` (rebind with a different `team_name`). A bind is refused when the named Team is missing or closed, and it mutates no routing state. Inbound submissions now pass display attributes and faithful body text through `team.submit` and Core renders the channel envelope."
          },
          {
            "comment": "BREAKING: Review: watch one COT card end-to-end after upgrading, including one input that fails before a runtime accepts it — that card now reads 任务失败 instead of 已中断. No config, state, cache, or path shape changed and no rebuild is needed. The session now subscribes to `teammate.input` and `teammate.activity` instead of the five deleted turn-scoped kinds, and recognizes its own inbound body by comparing `source_id` against the ids this session issued rather than through a two-hop turn correlation. A card's one terminal is a `turn.ended` activity, which prints its reason on the card before ending it. That terminal is now three-valued across two AG-UI events: a `completed` end sends `RUN_FINISHED done`, an `interrupted` end and every lifecycle interrupt send `RUN_FINISHED interrupted`, and a `failed` end sends `RUN_ERROR`, which is what the Feishu client renders as 任务失败 — a submission no runtime accepted used to end the card as 已中断. Open tool calls are keyed on the runtime's call id alone."
          }
        ],
        "minor": [
          {
            "comment": "COT tool rows show what the runtime said about the call: the row title is the runtime's summary (with a verb for read/list/search/edit rows and the tool name for rows without an action), the row carries the documented built-in icon for its action (bash/read/write/search), raw JSON arguments are no longer written into any row (a call nothing could label, such as an MCP tool, shows its name behind the icon-library token app-default_outlined with its arguments hidden), and the expanded result shows the invocation as a code segment before the output. A read or edit that succeeded expands to the files it touched as pills (a +N pill stands for those past the pill budget) and nothing else; a failed one keeps its diff and output after the pills. Result code segments now use the documented code field. An output that parses as a JSON object or array is pretty-printed in a json code segment; any other output is plain text (previously every output was a text code segment, with a one-line output under 120 bytes shown inline). Every card string is now cut only where it would exceed Feishu's 4,096-byte per-event content limit, with the truncation marker; the former 512-byte invocation and title soft caps and the 1,024-byte output soft cap are gone. The card's own words are now English: row verbs (Read/List/Search/Edit), the Completed/Failed status lines, and the truncation marker. The Channel no longer composes titles or result text for Dreamux's teammate tools (spawn, send, close, workflow_run) from their arguments, nor for its own reply, react and list_chat_bots tools; all of those show as unlabelled tool rows behind the generic app icon."
          },
          {
            "comment": "Render the COT card's opening label with a glyph Feishu shows as text. The previous character is in the Unicode emoji set, so Feishu replaced it with a coloured emoji tile; the replacement is a dingbat with no emoji presentation. Labels and behaviour are otherwise unchanged."
          },
          {
            "comment": "Open a Feishu COT card with one of five flavour labels chosen independently per card, replacing the fixed line that said processing had started. That line was written before Core was called, so it asserted an admission the Channel could not yet know; an ambiguous submission may never reach the runtime. The opening line now states nothing about admission or progress. Cards are append-only, so the label is picked once when the card opens and does not animate. This is an intentional operator-visible presentation change to a deliberately tuned surface."
          },
          {
            "comment": "Add the `ask_user_question` MCP tool: the model sends a Feishu decision card (1-4 single-select questions, each option carrying a label and a description) and the tool returns as soon as the card is sent. The answer is not awaited — a click settles the round server-side and the result arrives as an ordinary inbound message, so a user may take as long as they like. An optional `message_id` addresses the card under the message the question came out of, the way a reply is addressed. A round closes itself before Feishu stops accepting clicks on the card and tells the model no answer came."
          }
        ],
        "patch": [
          {
            "comment": "Derive the automatic-provisioning `team.create` request id from the inbound Feishu message that triggered it, instead of minting a fresh UUID per attempt. A message the platform redelivers now replays the same request id, so Core's idempotency answers with the Team the first attempt created rather than provisioning a duplicate for one conversation; when the first attempt died between creating the Team and installing the route, the replay reports the existing Team and the run finishes the binding, recovering instead of duplicating. Request identity is scoped to the message rather than the topic on purpose: a new message after a Team is dissolved mints a new id, so the same topic provisions a fresh Team normally. A different message arriving after a partial failure still creates a second Team, a window left undefended by design, and a redelivered id whose space policy changed in between fails loud as an idempotency conflict, reporting the message unsubmitted so it falls back to the Dispatcher Agent."
          }
        ],
        "none": [
          {
            "comment": "Documentation-only: update knowledge-base citation paths in the package README after the .agents reorganization; no package behavior or version change."
          }
        ],
        "dependency": [
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-types\" to `0.9.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-utils\" to `0.5.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/feishu-transport\" to `0.9.0`"
          }
        ]
      }
    },
    {
      "version": "5.0.0",
      "tag": "@excitedjs/feishu-channel_v5.0.0",
      "date": "Tue, 25 Aug 2026 11:45:34 GMT",
      "comments": {
        "major": [
          {
            "comment": "BREAKING: Review: before deploying, inspect every existing non-empty group.allow_chats entry under both group.policy=allowlist and group.policy=follow-user; keep only groups whose human membership should be trusted and whose passive known-bot observation should remain enabled. Previously, follow-user ignored allow_chats for ordinary human delivery, while allowlist still applied dm_policy and allow_users after the chat check. Now either non-block policy trusts every exactly classified human in a listed chat after the global mention gate. access.json remains V3 and needs no rebuild; the new authorization meaning takes effect in place when the new server starts. The package-root gate input remains unchanged, and callers must classify an exact human before passing is_bot_sender=false."
          },
          {
            "comment": "BREAKING: Publish complete Feishu MCP tool metadata and closed input/output schemas. Successful reply and react calls now return direct canonical message_ids and reaction_id objects, while live and sessionless list_chat_bots calls return the same direct chat_id, known, and trusted result."
          },
          {
            "comment": "BREAKING: Review: Channel contract consumers must use status-only delivery results, treat admission-ambiguous as terminal and non-replayed, and stop subscribing to the removed turn.submitted and turn.settled events. Feishu delivery no longer exposes Dreamux Turn IDs. No rebuild is required: Feishu config, access state, bindings, and transport state formats are unchanged."
          }
        ],
        "dependency": [
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-types\" to `0.8.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-utils\" to `0.4.0`"
          }
        ]
      }
    },
    {
      "version": "4.1.1",
      "tag": "@excitedjs/feishu-channel_v4.1.1",
      "date": "Mon, 27 Jul 2026 08:35:50 GMT",
      "comments": {
        "dependency": [
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-types\" to `0.7.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-utils\" to `0.3.4`"
          }
        ]
      }
    },
    {
      "version": "4.1.0",
      "tag": "@excitedjs/feishu-channel_v4.1.0",
      "date": "Sun, 26 Jul 2026 02:44:44 GMT",
      "comments": {
        "minor": [
          {
            "comment": "Expose caller-owned cancellation for interactive card sends and document timeout outcomes as remotely unknown."
          }
        ],
        "patch": [
          {
            "comment": "Send independent best-effort Feishu cards for binding route and collaboration-space transitions, retrying a failed or timed-out attempt once in memory."
          }
        ],
        "dependency": [
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-types\" to `0.6.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-utils\" to `0.3.3`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/feishu-transport\" to `0.8.0`"
          }
        ]
      }
    },
    {
      "version": "4.0.0",
      "tag": "@excitedjs/feishu-channel_v4.0.0",
      "date": "Thu, 23 Jul 2026 18:09:54 GMT",
      "comments": {
        "major": [
          {
            "comment": "BREAKING: The public Feishu formatter now returns structured Channel-owned content/ref XML, renders downloaded inline attachments as path-only elements and non-downloaded attachments as status/key-only elements, and renders create time as process-local unpadded time instead of UTC ISO. Accepted unnamed human turns may await a best-effort contact lookup for up to two seconds. Consumers that parse formatter body text, attachment attributes, or ISO timestamps must adopt the new contracts."
          }
        ],
        "patch": [
          {
            "comment": "Make Feishu channel acknowledgements conditional so immediately answerable requests receive a single direct reply."
          }
        ],
        "dependency": [
          {
            "comment": "Updating dependency \"@excitedjs/feishu-transport\" to `0.7.0`"
          }
        ]
      }
    },
    {
      "version": "3.0.0",
      "tag": "@excitedjs/feishu-channel_v3.0.0",
      "date": "Sun, 19 Jul 2026 03:45:02 GMT",
      "comments": {
        "minor": [
          {
            "comment": "Add backwards-compatible Feishu topic-group projection, fail-safe chat-mode verification, and exact topic reply authorization."
          },
          {
            "comment": "Preserve enclosing group bindings for non-collaboration topic groups and expose inbound thread_id to the model-visible channel envelope."
          }
        ],
        "major": [
          {
            "comment": "BREAKING: Remove the root-exported createFakeFeishuBot test helper and FakeFeishuBot type. Tests must provide package-local FeishuBot doubles; runtime behavior and persisted state are unchanged."
          }
        ],
        "dependency": [
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-types\" to `0.5.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-utils\" to `0.3.2`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/feishu-transport\" to `0.6.0`"
          }
        ]
      }
    },
    {
      "version": "2.1.0",
      "tag": "@excitedjs/feishu-channel_v2.1.0",
      "date": "Wed, 15 Jul 2026 02:54:37 GMT",
      "comments": {
        "minor": [
          {
            "comment": "BREAKING: Stop authoring channel MCP descriptors in the Feishu provider. The package now exposes only the static Feishu tool catalog plus reply/react/list_chat_bots handlers; Dreamux core renders the channel-mcp descriptor, admin socket, caller, team, and leader routing args."
          }
        ],
        "patch": [
          {
            "comment": "Clear the optimistic Feishu inbound received reaction when core submission throws before delivery, preventing a stale received marker on failed inbound turns."
          }
        ],
        "dependency": [
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-types\" to `0.4.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-utils\" to `0.3.1`"
          }
        ]
      }
    },
    {
      "version": "2.0.0",
      "tag": "@excitedjs/feishu-channel_v2.0.0",
      "date": "Fri, 03 Jul 2026 04:51:35 GMT",
      "comments": {
        "major": [
          {
            "comment": "BREAKING: remove the Feishu `access` MCP tool and make Owner-only interactive cards the only pairing approval surface. The channel now sends an interactive approval card for pending pairing requests, rejects non-Owner clicks with a toast-only response, and lets the App Owner approve through the card callback. The callback reuses the internal approve-by-token helper, then returns Feishu's raw-card callback response for the green success state. Existing callers must stop invoking the removed `access` tool; inspect or revoke pending entries by editing access.json directly."
          }
        ],
        "dependency": [
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-types\" to `0.3.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-utils\" to `0.3.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/feishu-transport\" to `0.5.0`"
          }
        ]
      }
    },
    {
      "version": "1.0.0",
      "tag": "@excitedjs/feishu-channel_v1.0.0",
      "date": "Sat, 27 Jun 2026 12:09:24 GMT",
      "comments": {
        "minor": [
          {
            "comment": "The Feishu channel session now exposes provider-owned target resolution: `FeishuChannelSession.resolveTarget(meta)` maps a `{ chat_id, chat_type }` selector to a neutral `ChannelTarget` (group chats are bindable, P2P chats are not; `target_key` is the chat id). This is the provider half of Dreamux core's binding store v2 + `(channel_id, target_key)` routing (issue #209). Additive — existing session behavior is unchanged; the neutral `ChannelSession` wrapper now delegates to this method instead of duplicating the mapping."
          },
          {
            "comment": "Promote @excitedjs/feishu-channel from scaffold to the publishable built-in Feishu ChannelProvider package (alias builtin:feishu, issue #209 slice 5). It now owns the live Feishu channel session, access/trust behavior, inbound normalization, attachment handling, target resolution, MCP descriptor, and provider tool backing (`reply` / `react` / `list_chat_bots`), implementing the neutral @excitedjs/dreamux-types ChannelProvider/ChannelSession contract on top of @excitedjs/feishu-transport. Depends on @excitedjs/dreamux-types + @excitedjs/feishu-transport only — never on @excitedjs/dreamux core. The host supplies provider-validated config plus neutral state/cache roots through the session context; core keeps only routing, binding state, authorization, and the generic admin conduit."
          },
          {
            "comment": "The Feishu channel provider's `readConfig` is now the config-validation authority for `builtin:feishu` (issue #209 multi-channel config): Dreamux core registers this provider and calls `readConfig` at config load instead of validating Feishu fields itself. `readConfig` now fails loud on an empty/whitespace `app_id` or `app_secret` (the bot secret is config-sourced, so this preserves the host's previous config-load fail-loud) and on unknown config keys. Parsing of a valid `{ app_id, app_secret }` into `{ appId, appSecret }` is unchanged."
          },
          {
            "comment": "The built-in Feishu Channel provider now owns onboarding prompts for bot app id and app secret and exposes a provider diagnostic capability. Dreamux core records the returned raw config and drives the channel through the shared provider diagnostic contract instead of hardcoding Feishu fields in onboard or doctor."
          }
        ],
        "patch": [
          {
            "comment": "The Feishu provider's `mcpServerDescriptor` now targets Dreamux core's generic channel-tool shim: descriptor args change from `feishu-mcp ...` to `channel-mcp --provider builtin:feishu --channel-id <id> --dispatcher <id> ...`. The MCP server name (`feishu`) and provider tool surface (`reply` / `react` / `list_chat_bots`) are unchanged. The shim serves the provider's static `tools/list` metadata and forwards `tools/call` to the neutral `channel.invoke_tool` admin method; binding remains on the Team MCP and is not a channel-tool responsibility. Rebuild: none required — descriptors are regenerated at every dispatcher boot, never persisted."
          },
          {
            "comment": "Type the package's default factory export against the published `ChannelProviderFactory` contract and use the kind-narrowed `ChannelProviderDescriptor` for the builtin descriptor (issue #209 types-API audit). No runtime behavior change."
          },
          {
            "comment": "Adopt the shared @excitedjs/eslint-config flat config and the synchronous-blocking-IO lint gate (issue #85); no runtime change"
          }
        ],
        "major": [
          {
            "comment": "BREAKING: Replace the v2 access gate (inline `FeishuChannelSession.onMessage` branches + transport `policy/gate.ts` + `contract/access-store.ts`) with the access-gate-v3 pairing-code mechanism. The persisted dispatcher access state now uses `DispatcherAccessStateV3` (`version: 3`) and the loader fails loud when an older version is detected — no silent migration.\n\nRebuild: Your existing v2 dispatcher access file (usually `<dispatcher-state-root>/feishu/dispatcher_access.json`) is **not** deleted automatically — the v3 loader crashes loudly on v2. Preserve your existing access list before upgrading, then create a fresh state file. From your current v2 file, copy the following fields verbatim into a new file named `access.json` in the same directory: `allow_users`, `group.allow_chats`, `group.require_mention`, then add `\"version\": 3`, `\"dm_policy\": \"allowlist\"`, `\"pending\": {}`, `\"known_banned_senders\": []`. A sibling `chat-bots.json` will be created on first session start from the in-memory chat-bots ledger; the former v2 inline chat_bots entries are NOT ported automatically (it is OK to lose them; the channel rebuilds the ledger live from the first few bot encounters — no data loss for pairing or allowlist behavior).\n\nHighlights:\n- 22-line table-driven `dreamuxFeishuGate()` pure function covering (DM × 5 policies × pairing-resend) + (group × 5 policies × mention gate × pairing-resend × trusted-bot + bot-self) branches.\n- `require_mention: true` default (product decision).\n- Follow-user strangers in a group who @-mention the bot now receive a pairing code (product decision — no cooldown / default-drop switch).\n- Per-kind pending-pairing quota: `MAX_PENDING_PER_KIND = 10`, independent for DM vs group, with expired entries excluded before the count.\n- TTL double guard: `pruneExpiredPending` at gate entry + inline `expires_at > now` check inside `findExistingPendingByKey`.\n- Pairing-code prompt rendered with the bot display name and a 6-hex code; resends use the same code.\n- LOCK-SEND-LOCK pattern on the pairing path: gate + first save under `AsyncMutex`, then `sendText` outside the lock, then re-enter the lock to read the latest state and only persist the fresh pending entry when the send succeeded and no earlier concurrent entry already covers the same (kind, key) slot.\n- `approvePairingByCode` is idempotent on membership (duplicate approve → reported as `duplicate: true`; pending key is always removed).\n- MCP `access` tool now has a real handler; `reply` / `react` / `list_chat_bots` / `access` centralized in `tools/registry.ts`.\n- Atomic write for the access-state file (tmpfile + rename, mode `0o600`; directory created at mode `0o700`).\n- `TRUST_DOMAIN_WARNING` exported from the barrel index for operator-facing UIs.\n- Transport hard boundary: `@excitedjs/feishu-transport` no longer ships any access-control or persistence logic."
          }
        ],
        "dependency": [
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-types\" to `0.2.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/dreamux-utils\" to `0.2.0`"
          },
          {
            "comment": "Updating dependency \"@excitedjs/feishu-transport\" to `0.4.0`"
          }
        ]
      }
    }
  ]
}
