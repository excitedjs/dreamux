/**
 * Re-export shim: the Codex `initialize` handshake now lives in
 * `@excitedjs/agent-runtime-codex` (issue #209 slice 3).
 */
export { performInitializeHandshake } from '@excitedjs/agent-runtime-codex';
